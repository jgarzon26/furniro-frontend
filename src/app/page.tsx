import { CategorySection, IntroSection, ProductSection, DisplaySection } from "@/components/home";

const HomePage = () => {
  return (
    <main>
      <IntroSection />
      <CategorySection />
      <ProductSection />
      <DisplaySection />
    </main>
  );
}

export default HomePage;